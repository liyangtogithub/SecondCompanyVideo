<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="@dimen/grid_item_width"
    android:layout_height="@dimen/grid_item_height"
    android:layout_marginLeft="2dp"
    android:layout_marginBottom="3dp"
    android:focusable="true"
    android:background="@drawable/selector_thumb_backgrount"
    android:descendantFocusability="blocksDescendants"
     >
     
     <ImageView 
          android:id="@+id/iv_thumb_id"
          android:layout_width="match_parent"
          android:layout_height="match_parent"
          android:layout_margin="@dimen/border_width"
          android:background="#ffffff"
         
         />
    
<!-- 	<TextView 
	    android:id="@+id/tv_thumb_id"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:background="@drawable/ic_list_item_type_folder"
	    android:gravity="center"
	    android:textColor="@color/grid_list_item_title_text"
	    android:singleLine="true"
	    android:ellipsize="end"
	    />
	    android:layout_alignBottom="@+id/tv_thumb_id"
	    android:layout_alignLeft="@+id/tv_thumb_id"
	    android:layout_alignRight="@+id/tv_thumb_id"
	 -->
	<TextView 
	    android:id="@+id/tv_thumb_title_id"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:textColor="@android:color/white"
	    android:singleLine="true"
	    android:ellipsize="none"
	    android:background="@drawable/ic_list_item_title_bg"
	    android:layout_alignParentBottom="true"
	   	android:layout_alignParentLeft="true"
	   	android:layout_alignParentRight="true"
	    android:gravity="right|center_vertical"/>
    
</RelativeLayout>
